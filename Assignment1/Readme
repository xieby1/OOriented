TIP：运行Painter的main函数
使用方法：在文本框中直接输入金额和红包数量，然后点塞入红包即可在控制台中显示每个红包的金额。
特性：
      +金额框中只能输入0.00~999.99的数
      +数量框中的值只能为正整数且要保证每个红包至少有0.01元，
        ·例如金额框中的值为0.05，则数量框的范围为1~5；
        ·如果输入数量5，然后把金额改为0.04，则数量框中的值会自动被限制为4
      +多次点击“塞进红包”可以在控制台得到多次不同的分配，但是每个人被分配到的钱的期望是一样的。
 Xie Benyi
 2017.9.23
      
